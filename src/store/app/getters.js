export const isLogged = state => state.user.logged;

export const getUser = state => state.user;

export const getUserProfile = state => state.user.profile;

export const getUserTokens = state => state.user.tokens;
